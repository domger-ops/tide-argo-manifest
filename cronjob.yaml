apiVersion: batch/v1
kind: CronJob
metadata:
  name: tide-app-cron
spec:
  schedule: "10 8 * * *"
  jobTemplate:
    spec:
      template:
        spec:
          containers:
          - name: tide-app
            image: gerops/tide-app:a3a9afb
            imagePullPolicy: IfNotPresent
          restartPolicy: OnFailure
          imagePullSecrets:
          - name: docker-secret
          
---