apiVersion: batch/v1
kind: CronJob
metadata:
  name: weather-app-cron
spec:
  schedule: "0 5,17 * * *"
  jobTemplate:
    spec:
      template:
        spec:
          containers:
          - name: weather-app
            image: gerops/weather-app:00190f2
            imagePullPolicy: IfNotPresent
          restartPolicy: OnFailure
          imagePullSecrets:
          - name: docker-secret
          
---